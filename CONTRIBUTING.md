# 参与 VeilCheck

谢谢你愿意帮忙。这个项目最需要的不是更多“看起来很聪明”的功能，而是更少的漏报、更清楚的提示，以及普通人不会用错的流程。

## 提交问题前

请先搜索已有 Issue。反馈识别问题时，不要上传真实文件，也不要把真实姓名、手机号、证件号打码后再上传——不完整的打码本身就可能泄露信息。

可以这样制作复现材料：

- 用虚构姓名和明显的测试号码重新做一张图片
- 用 `example.com` 邮箱和无效账号
- 描述版式、字体、旋转角度和压缩程度
- 提供能生成测试文件的脚本

## 本地开发

需要 Node.js 20+ 和 pnpm。

```bash
pnpm install
pnpm start
```

提交前运行：

```bash
pnpm check
pnpm test
pnpm benchmark
```

修改图片或 PDF 导出流程时，还应该亲自打开生成的文件，确认遮挡位置、页数和视觉效果正确。不要只检查“程序没有报错”。

## 代码和产品原则

1. 默认本地处理。新增远程请求前必须先说明必要性、发送内容和关闭方式。
2. 不把“没有检测到”写成“绝对安全”。
3. 高敏内容优先保证不可恢复，而不是只追求视觉效果。
4. 错误提示要告诉用户下一步能做什么。
5. 测试文件只能使用合成数据或许可明确的公开数据。

## Pull Request

一个 PR 尽量只解决一个问题。请说明用户场景、改动内容和验证方法。界面改动请附截图；检测规则改动请同时增加正例和负例测试，避免为了召回率制造大量误报。
