# Changelog

## 0.6.1

- 重写 README，用实际使用场景解释项目、操作流程、遮挡方式和安全边界。
- 新增项目截图、隐私说明、贡献指南、安全报告流程和第三方许可说明。
- 新增 GitHub Actions，自动执行语法检查、单元测试和合成基准。
- 新增 Bug、功能建议和 Pull Request 模板，并明确禁止提交真实隐私文件。
- 新增 `.gitignore` 与 `.gitattributes`，避免提交依赖、输出文件、环境变量和系统文件。
- 移除 Windows 启动脚本中的本机绝对路径，改为通用的 Node.js 检查和错误提示。

## 0.6.0

- 文件入口新增 PDF 支持，在本地完成解析、页面渲染和敏感信息识别。
- 新增 PDF 上一页/下一页导航，每一页独立保存检测框、手动画框和编辑结果。
- 导出时逐页栅格化并重建整份 PDF，移除原文字层、批注、表单和来源元数据，避免“只盖黑框”的伪脱敏。
- 安全 PDF 支持第一页预览、整份下载、跨页 OCR 二次复检和隐私检查报告。
- 新增两页合成 PDF 回归样本，验证跨页检测、6 个遮挡区域、页数保留和导出文件零可提取文字层。
- 扩大上下文字段的右侧安全余量，并兼容 OCR 将“学号”识别成“学叶”的常见错误。
- 本地内置 PDF.js 与 pdf-lib 运行文件及许可证，PDF 功能不依赖 CDN 或云端 API。

## 0.5.0

- 遮挡框支持点击选择、拖动移动、四角缩放、Delete 删除以及最多 50 步撤销/重做。
- OCR 重复扫描新增空间重叠和归一化内容去重，避免相同结果不断叠加。
- 安全副本改为先在页面预览，再由用户主动下载；修改区域或遮挡参数会使旧预览失效。
- 新增安全副本 OCR 二次复检，可提示仍能识别出的字段并建议扩大区域或改用不透明遮挡。
- 新增二维码与常见条码的浏览器本地检测，并为不支持的浏览器提供清晰降级提示。
- 新增不含原始敏感值、图片内容和坐标的 JSON 隐私检查报告。
- 扩充真实浏览器回归流程，覆盖去重、编辑、撤销/重做、四种导出、复检和报告隐私检查。

## 0.4.0

- 将遮挡方式从下拉菜单改为四个可视化样式卡片，马赛克可直接选择。
- 大幅提高马赛克默认块尺寸，并加入弱、中、强、极强四档连续调节。
- 新增强度可调模糊效果，并明确标记为低安全级别。
- 新增带不透明底层和“已隐藏”字样的隐私标签遮挡。
- 实心遮挡支持自定义颜色，适配黑底、白底及文档场景。
- 实心与隐私标签继续执行导出前像素写入验证；马赛克和模糊显示不可逆性风险提示。

## 0.3.0

- 新增通用“敏感字段标签—对应内容”检测，而非只依赖固定格式正则。
- 默认覆盖姓名、学号/考号、身份证字段、电话、邮箱、地址、账号/工号、出生日期、性别、学院、专业和班级。
- OCR 可查找标签右侧或下方的值，适配表格型成绩单和证件。
- 新增可配置的额外敏感字段，例如导师、宿舍号和项目代号。
- 加入合成中文成绩单与字段上下文回归测试。

## 0.2.0

- 加入身份证日期与校验位验证，减少无效号码误报。
- 支持带空格或连字符的手机号。
- 增加 AWS Access Key 与私钥头检测。
- OCR 结果可重组被分成多个词块的手机号和邮箱。
- 扩大 OCR 遮挡安全边界，避免首尾字符残留。
- 实心遮挡导出前执行像素写入验证，失败时阻止下载。
- 对像素化遮挡增加高敏信息风险提示。
- 加入 25 条合成基准、OCR 图片样本和 precision/recall/F1 报告命令。
- 验证桌面端与 390px 移动端布局无横向溢出。

## 0.1.0

- 首个可运行版本：本地图片 OCR、文本检测、手动画框和 PNG 安全副本导出。
