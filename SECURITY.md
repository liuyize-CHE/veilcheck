# 安全与隐私问题

VeilCheck 处理的内容可能非常敏感，因此安全问题包括传统漏洞，也包括会让用户误以为文件已经安全的行为。

## 请私下报告这些问题

- 导出的文件仍能恢复被遮挡的原始内容
- 文件或识别结果被发送到本机之外
- 路径穿越、任意文件读取或本地服务暴露到局域网
- 恶意图片或 PDF 能执行脚本、读取本地数据或导致严重资源耗尽
- 界面把失败状态显示成成功

公开仓库启用后，请使用 GitHub 的 **Security advisories / Report a vulnerability** 私下报告。不要在公开 Issue 中附上漏洞利用代码或真实隐私文件。

## 支持范围

优先修复最新发布版本。旧版本可能只获得迁移建议。安全修复完成前，不会公开披露能直接恢复用户隐私内容的细节。

## 数据处理边界

应用只监听 `127.0.0.1`，正常流程不需要云端 API 或遥测。自动检测和二次复检都可能误报、漏报，用户仍需检查预览。更多说明见 [docs/PRIVACY.md](docs/PRIVACY.md)。
